목표: 요구사항을 충족하는 최소 변경으로 구현하고, 불필요한 탐색·출력·재작업을 줄인다.

작업 원칙

변경 전에 관련 코드와 호출 흐름을 읽고, 성공 기준을 정한다.

정책·데이터 정합성에 영향을 주는 모호함은 확인한다. 일상적인 구현 선택은 기존 패턴을 따른다.

기존 코드 → 표준 라이브러리·플랫폼 기능 → 설치된 의존성 순으로 재사용 가능성을 확인한다.

요청 범위만 수정한다. 필요성이 확인되지 않은 기능·추상화·의존성을 추가하지 않는다.

관련 없는 리팩터링과 서식 변경을 피하고, 이번 변경으로 불필요해진 코드만 정리한다.

코드 길이를 줄이려고 가독성, 검증, 보안, 오류 처리, 정합성을 희생하지 않는다.

토큰 사용

검색으로 관련 파일과 필요한 구간을 좁힌다. 변경이나 새로운 확인 사유가 없으면 같은 내용을 반복해서 읽지 않는다.

상세 설계·운영 문서는 관련 작업에 필요한 부분만 읽는다.

명령 출력은 오류와 판단에 필요한 부분을 중심으로 확인하되, 종료 코드와 실패 여부를 보존한다.

단순 작업은 바로 수행하고, 복잡한 작업만 짧은 계획을 세운다. 작은 작업을 여러 에이전트로 나누지 않는다.

답변은 변경 내용·검증 결과·남은 문제 중심으로 짧게 쓴다. 전체 코드와 이미 설명한 내용을 반복하지 않는다.

쿠폰 프로젝트의 불변 규칙

환경은 Java/Spring 인스턴스 2개, Redis, MySQL이다. 제안된 설계와 실제 구현을 구분한다.

행사당 회원 1장, 발급 한도 준수, 설정된 시작 시각 이전 발급 금지를 보장한다.

합의된 서비스 접수 순서를 유지한다. 락 획득 순서나 작업 완료 순서를 선착순으로 간주하지 않는다.

접수 완료를 안내한 신청은 저장소가 보존되는 프로세스 종료·재시작 상황에서도 순서와 함께 복구해야 한다.

재고 변경·쿠폰 생성 또는 배정·신청 결과가 부분적으로만 확정되지 않게 한다. 재시도에는 기존 신청·쿠폰·결과를 반환한다.

발급과 포인트 적립은 별개다. 발급 후 7일 이내 쿠폰 사용 시 1만 포인트를 한 번만 적립하고, 포인트는 사용 시점부터 1개월간 유효하다.

장애로 결과가 불확실하면 확인 중으로 안내한다. 기록 보존·수량·순서를 지키기 위해 처리 지연을 허용한다.

분산 락이나 캐시만으로 중복·초과 발급 방지, 순서 보존, 장애 복구가 보장된다고 가정하지 않는다.

검증과 보고

버그는 재현 조건을 확보하고, 변경 범위에 맞는 검증을 수행한다. 사소한 변경에 불필요한 테스트를 늘리지 않는다.

발급·사용·재처리 변경에는 해당하는 동시 요청, 중복 요청, 커밋 전후 장애, 응답 유실 시나리오를 확인한다.

쿠폰의 발급 시각과 사전 생성 시각을 구분한다. 쿠폰·포인트 만료 경계를 검증한다.

부하 검증 목표는 10초간 최대 1만 명 신청, 정상 시 접수 결과 p95 2초 이내·최종 결과 3분 이내다. 재시도와 상태 조회 부하도 명시한다.

실행한 검증만 보고한다. 미실행·실패·환경 제약을 구분하고, 측정 결과를 검증하지 않은 환경으로 일반화하지 않는다.