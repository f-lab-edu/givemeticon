<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jinddung2.givemeticon.domain.coupon.mapper.CouponAwardMapper">
    <sql id="columns">
        id, application_id, event_id, member_id, tier, points, status, issued_at, redeemed_at
    </sql>

    <select id="findByApplicationId" resultType="CouponAward">
        SELECT <include refid="columns"/>
        FROM coupon_award
        WHERE application_id = #{applicationId}
    </select>

    <insert id="insert" parameterType="CouponAward" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO coupon_award (
            application_id, event_id, member_id, tier, points, status, issued_at
        ) VALUES (
            #{applicationId}, #{eventId}, #{memberId}, #{tier}, #{points}, #{status}, UTC_TIMESTAMP(6)
        )
    </insert>
</mapper>
